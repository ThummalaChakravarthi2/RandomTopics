Scanner sc = new Scanner(System.in);

int x = sc.nextInt();
sc.nextLine();   // consume the leftover newline

String str = sc.nextLine();  // now read the actual line
// Space (' '): just a blank gap between words on the same line.

// Newline (\n): means move the cursor to the next line (like when you press Enter)
